---
# Source: valid/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
    # Single-quoted string normalized
    name: 'my-release'
    # Block scalar string normalized
    namespace: |
      my-namespace
# Comments normalized (dropped)
spec:
  selector:
    version: v1
    app: my-release
  ports:
    # Double-quoted string normalized
    - name: "my-service"
      # key ordering normalized (sorted)
      targetPort: 1234
      port: 1234
